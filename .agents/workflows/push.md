---
description: Automatically commits and pushes all changes to GitHub instantly.
---
// turbo-all

1. Stage all changes by running `git add .`
2. Commit the changes by running `git commit -m "Automated update"` 
3. Push to the remote repository by running `git push`
